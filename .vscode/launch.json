{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch video-game-api",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/video-game-api/main.go",
            "envFile": "${workspaceFolder}/.env",
        }
    ]
}